@model dynamic

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div>
    <h2>User Claims</h2>
    <ul>
        @foreach (var claim in ViewBag.Claims)
        {
            <li><strong>@claim.Type:</strong> @claim.Value</li>
        }
    </ul>
</div>
